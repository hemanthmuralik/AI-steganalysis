name: Model Quality Check

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install tensorflow numpy opencv-python
        
    - name: Verify Model Architecture
      run: |
        # Creates a quick script to check if the model compiles
        python -c "from models.ye_net import build_yenet; model = build_yenet(); print('Ye-Net Compiled Successfully ✅')"
